<%= form_with(model: address, local: true) do |form| %>
  <% errors = Shared::ShowErrs.new(address) %>
  <% input = Shared::Inputs.new(form) %>

  <%= errors.show %>

  <%= input.row([ { attribute: :person_id, kind: :select,
                    collection: adata.persons_list,
                    selected: address.person_id,
                    disabled: address.person_id },
                  { attribute: :person_id, kind: :hidden,
                    value: address.person_id } ]) %>

  <%= input.row([ { attribute: :number },
                  { attribute: :street } ]) %>

  <%= input.row([ { attribute: :city },
                  { attribute: :state, width: 1 },
                  { attribute: :zip,   width: 1 } ]) %>

  <%= input.row([ { attribute: :address_type, kind: :select,
                    label: 'Address Type',
                    collection: adata.types,
                    selected: address.address_type,
                    blank: false },
                  { attribute: :preferred, kind: :select,
                    collection: [['Yes',true],['No',false]],
                    selected: address.preferred,
                    blank: false } ]) %>

  <%= input.row([ { attribute: :note, kind: :textarea } ]) %>

  <%= render  partial: 'shared/form_submit_bs',
              locals: { form: form } %>
<% end %>
